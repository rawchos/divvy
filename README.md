# divvy

A simple command line application that will `divvy` up a passed-in interest amount among eligible accounts. The amount for each account will be proportional to the current value of the account.
